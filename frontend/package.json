{
  "name": "cursorgame-frontend",
  "version": "1.0.0",
  "description": "Phaser frontend for cursorgame, integrated with a Colyseus backend",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@colyseus/schema": "^5.0.9",
    "colyseus.js": "^0.16.22",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.1.5"
  }
}
