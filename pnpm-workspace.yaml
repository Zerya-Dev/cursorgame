packages:
  - "frontend"
  - "backend"

# uWebSockets transport depends on the official GitHub package.
blockExoticSubdeps: false

# Allow only these dependencies to run install/build scripts.
allowBuilds:
  esbuild: true
  msgpackr-extract: false
